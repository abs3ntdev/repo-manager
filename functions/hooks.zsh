post_repo_clone() {
  cd "$1" || return
}

post_repo_goto() {
  cd "$1" || return
}

post_repo_new() {
  cd "$1" || return
}

post_wt_add() {
  cd "$1" || return
}

post_wt_go() {
  cd "$1" || return
}

post_wt_rm() {
  :
}
